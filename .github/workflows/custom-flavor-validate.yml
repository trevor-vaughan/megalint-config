---
name: Custom Flavor Validation

# yamllint disable-line rule:truthy
on:
  pull_request:
    paths:
      - ".mega-linter.yml"
      - ".taskfiles/flavor.yml"
      - ".github/workflows/custom-flavor-validate.yml"
      - ".github/workflows/custom-flavor-release.yml"
      - "action.yml"
      - "scripts/**"
      - "tests/**"
      - "pyproject.toml"
      - "uv.lock"
  push:
    branches-ignore:
      - "main"
    paths:
      - ".mega-linter.yml"
      - ".taskfiles/flavor.yml"
      - ".github/workflows/custom-flavor-validate.yml"
      - ".github/workflows/custom-flavor-release.yml"
      - "action.yml"
      - "scripts/**"
      - "tests/**"
      - "pyproject.toml"
      - "uv.lock"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-flavor:
    name: Validate Custom Flavor
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true

      - name: Install Task
        uses: go-task/setup-task@01a4adf9db2d14c1de7a560f09170b6e0df736aa # v2.1.0
        with:
          repo-token: ${{ github.token }}

      - name: Sync dependencies
        run: task dev:sync

      - name: Clone MegaLinter source
        run: task flavor:clone

      - name: Generate custom flavor
        run: task flavor:generate FLAVOR_NAME=test-flavor

      - name: Validate generated flavor
        run: task flavor:validate

      - name: Verify flavor structure
        run: |
          echo "=== Generated Flavor Structure ==="
          find ./custom-flavor -type f -name "*.py" -o -name "*.yml" -o -name "Dockerfile" | head -20
          echo
          echo "=== Dockerfile exists ==="
          test -f ./custom-flavor/Dockerfile && echo "✓ Dockerfile found" || echo "✗ Dockerfile missing"
          echo
          echo "=== mega-linter-flavor.yml exists ==="
          test -f ./custom-flavor/mega-linter-flavor.yml && echo "✓ mega-linter-flavor.yml found" || echo "✗ mega-linter-flavor.yml missing"

      - name: Archive generated flavor
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: custom-flavor-validation-${{ github.sha }}-${{ github.run_attempt }}
          path: ./custom-flavor/
          retention-days: 7
          if-no-files-found: error

  test-docker-build:
    name: Test Docker Build
    runs-on: ubuntu-latest
    needs: validate-flavor
    if: success()
    permissions:
      contents: read
    env:
      # This job builds with `docker build` directly, so every step
      # (including Taskfile tasks) must use the same engine.
      # ubuntu-latest ships podman, which Task would otherwise auto-select.
      CONTAINER_ENGINE: docker
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install Task
        uses: go-task/setup-task@01a4adf9db2d14c1de7a560f09170b6e0df736aa # v2.1.0
        with:
          repo-token: ${{ github.token }}

      - name: Clone MegaLinter source
        run: task flavor:clone

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@b5ca514318bd6ebac0fb2aedd5d36ec1b5c232a2 # v3.10.0

      - name: Download flavor artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: custom-flavor-validation-${{ github.sha }}-${{ github.run_attempt }}
          path: ./custom-flavor

      - name: Validate artifact contents
        run: |
          echo "=== Validating artifact contents ==="
          if [[ $(find ./custom-flavor -name "*/.." -o -name "../*" | wc -l) -gt 0 ]]; then
            echo "Error: Suspicious paths found in artifact"
            exit 1
          fi
          echo "Artifact contents validated"
          ls -la ./custom-flavor/

      - name: Verify Dockerfile exists
        run: |
          if [ ! -f ./custom-flavor/Dockerfile ]; then
            echo "Error: Dockerfile not found in artifact"
            exit 1
          fi
          echo "Dockerfile found, proceeding with build test"

      - name: Prepare Docker build context
        id: context
        run: |
          set -euo pipefail
          # The upstream Dockerfile references files from the MegaLinter repo
          # (pyproject.toml, megalinter/, etc.), so the build context must be
          # the cached MegaLinter source — same approach as `task flavor:build`.
          # Discover the cache dir rather than hardcoding the version.
          CACHE_DIR="$(find .cache -maxdepth 1 -name 'megalinter-v*' -type d | head -1)"
          if [ -z "${CACHE_DIR}" ]; then
            echo "Error: MegaLinter cache directory not found"
            exit 1
          fi
          cp ./custom-flavor/mega-linter-flavor.yml "${CACHE_DIR}/mega-linter-flavor.yml"
          echo "context=${CACHE_DIR}" >> "$GITHUB_OUTPUT"

      - name: Build Docker image
        env:
          IMAGE_TAG: test-custom-flavor:${{ github.sha }}-${{ github.run_attempt }}
          BUILD_REVISION: ${{ github.sha }}
          BUILD_CONTEXT: ${{ steps.context.outputs.context }}
        run: |
          docker build \
            -t "${IMAGE_TAG}" \
            -f ./custom-flavor/Dockerfile \
            --build-arg BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            --build-arg BUILD_REVISION="${BUILD_REVISION}" \
            --build-arg BUILD_VERSION="test" \
            "${BUILD_CONTEXT}"
        timeout-minutes: 30

      - name: Smoke-test image
        env:
          IMAGE_TAG: test-custom-flavor:${{ github.sha }}-${{ github.run_attempt }}
        run: task flavor:smoke-test IMAGE_NAME="${IMAGE_TAG%%:*}" IMAGE_TAG="${IMAGE_TAG##*:}"

      # Vulnerability scan is informational (exit-code: 0) because most
      # findings originate in the upstream MegaLinter base image and its
      # bundled language-specific packages, which we cannot patch.
      - name: Scan image for vulnerabilities
        env:
          IMAGE_TAG: test-custom-flavor:${{ github.sha }}-${{ github.run_attempt }}
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ env.IMAGE_TAG }}
          version: v0.70.0
          format: table
          severity: CRITICAL,HIGH
          exit-code: "0"
          ignore-unfixed: true

      - name: Clean up Docker image
        if: always()
        env:
          IMAGE_TAG: test-custom-flavor:${{ github.sha }}-${{ github.run_attempt }}
        run: |
          docker rmi "${IMAGE_TAG}" || true
