# TruffleHog path exclusions (shared template)
# Referenced by REPOSITORY_TRUFFLEHOG_ARGUMENTS in .mega-linter.yml.
# Regex patterns, one per line. Lines starting with # are comments.
# Docs: https://github.com/trufflesecurity/trufflehog#excluding-paths
#
# Target repos can override this by placing their own
# .trufflehog-exclude-paths.txt at the repo root. The runner skips
# overlay when the target already has a file with the same name.

^\.cache/
