# https://taskfile.dev
version: '3'

tasks:
  runner:
    desc: 'Run bats tests for .taskfiles/scripts/*.sh'
    cmds:
      - bats {{.ROOT_DIR}}/tests/*.bats

  default:
    cmds:
      - task: runner
